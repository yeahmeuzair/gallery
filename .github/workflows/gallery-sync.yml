name: Auto Update BISE Gallery JSON

on:
  schedule:
    - cron: '0 */6 * * *' 
  workflow_dispatch: 

permissions:
  contents: write 

jobs:
  update-gallery:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0 # This line prevents the fetch/push rejection error

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install feedparser requests

      - name: Run Gallery Scraper
        run: python gallery_scraper.py

      - name: Commit and Push Changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add gallery_data.json
          
          # Check for changes and commit
          git diff --quiet && git diff --staged --quiet || git commit -m "Auto-update gallery data"
          
          # Pull the latest changes from your repo safely, then push
          git pull --rebase origin main
          git push
